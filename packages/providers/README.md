# Provider Adapters

Contains the common provider interface and adapters for the deterministic mock provider, Ollama, and optional cloud providers.

Adapters must normalize streaming, usage, timeout, quota, cancellation, and error behavior.

