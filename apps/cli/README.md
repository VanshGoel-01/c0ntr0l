# CLI Workstream

Owns the Typer and Rich client commands: `top`, `inspect`, `kill`, `budgets`, and `incidents`.

The CLI must call public APIs rather than connecting directly to PostgreSQL or Redis.

