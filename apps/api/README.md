# API Workstream

Owns the FastAPI gateway, identity resolution, execution lifecycle, usage normalization, budgets, policies, tracing, incidents, security, and handoff coordination.

Public behavior must follow `docs/contracts/openapi.yaml` and `docs/contracts/events.md`.

